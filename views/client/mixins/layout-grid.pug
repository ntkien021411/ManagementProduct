include ./layout-grid-item.pug

mixin product-grid(products)
    .row 
        each item in products
            +product-item(item)
mixin post-grid(posts)
    .row
        each item in posts
            +post-item(item)
